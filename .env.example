# Both are optional locally. The deploy workflow sets them from actions/configure-pages.
# Canonical origin, used for <link rel=canonical>, Open Graph URLs, sitemap and robots.txt.
SITE_URL=
# Path prefix for project Pages (https://<user>.github.io/<repo>/). Leave empty for "/".
BASE_PATH=
