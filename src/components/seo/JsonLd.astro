---
import { serializeJsonLd } from '../../seo/person-schema.ts';

interface Props {
  data: unknown;
}

const { data } = Astro.props;
---

<script type="application/ld+json" set:html={serializeJsonLd(data)} is:inline></script>
